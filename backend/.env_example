# Example .env values — copy to .env and replace placeholders with real secrets

MONGO_URL="mongodb+srv://<DB_USER>:<DB_PASS>@cluster0.mongodb.net/<DB_NAME>?retryWrites=true&w=majority"

JWT_USER_SECRET="replace_with_strong_user_jwt_secret"
JWT_ADMIN_SECRET="replace_with_strong_admin_jwt_secret"

MAIL_HOST="smtp.gmail.com"
MAIL_USER="your-email@example.com"
MAIL_PASS="your-email-app-password-or-oauth-token"

# Optional / recommended
# FRONTEND_URL="https://your-frontend-domain.vercel.app"
# PORT=
